import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    title: {
        marginTop: 20,
        fontSize: 20,
        fontWeight: 'bold',
        textAlign: 'center',
        color: '#E5BF3C',
        textTransform: 'uppercase'
    }
})